import 'package:flutter/material.dart';

class kaprodiPage extends StatelessWidget {
  const kaprodiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
